# Roversa Streamlit Dashboard Final

This app:
- reads Google Form responses from the response sheet
- downloads each uploaded CSV file
- stores submissions and session rows in SQLite
- lets you filter by teacher, student, and class in Streamlit

## Setup

1. Install packages

```bash
pip install -r requirements.txt
